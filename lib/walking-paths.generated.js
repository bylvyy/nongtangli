// AUTO-GENERATED placeholder. Real content is produced by
// `scripts/precompute-walking.mjs` during build (npm run precompute).
// In environments where the precompute step can't reach AMap (e.g. local
// without network or first checkout), this empty object is fine — the
// client will fall back to fetching paths at runtime.

export const WALKING_PATHS = {};
